<?xml version="1.0" encoding="iso-8859-1" standalone="yes" ?>

<!-- 
    A sample installation file.
    Use it as a base for your own installers :-)
    
    To compile it :
    - go in the bin directory where you installed IzPack
    - call "compile ../sample/install.xml -b ../sample"
-->

<installation version="1.0">

    <!-- 
        The info section.
        The meaning of the tags should be natural ...
    -->
    <info>
        <appname>ECMOjo</appname>
        <appversion>2.4.0</appversion>
        <authors>
            <author name="Len Tanaka" email="lent@hawaii.edu"/>
            <author name="Christoph Aschwanden" email="caschwan@hawaii.edu"/>
        </authors>
        <url>http://ecmojo.sourceforge.net/</url>
	</info>

	<!--
	    The variable definitions. DOES NOT WORK?!
	-->
	<variables>
		<variable name="build" value="build/app/application"/>
		<variable name="rcsIzpack" value="resource/installer/izpack"/>
		<variable name="InstallerFrame.logfilePath" value="Default"/>
	</variables>

    <!-- 
        The gui preferences indication.
        Sets the installer window to 640x480. It will not be able to change the size.
    -->
    <guiprefs width="640" height="480" resizable="yes"/>

	<!-- 
        The locale section.
		Asks here to include the English and Japanese langpacks.
	-->
    <locale>
        <langpack iso3="eng"/>
		<!--<langpack iso3="jpn"/>-->
	</locale>

    <!-- 
        The resources section.
        The ids must be these ones if you want to use the LicencePanel and/or the InfoPanel.
    -->
    <resources>
        <res id="LicencePanel.licence" src="LICENSE.txt"/>
		<res id="InfoPanel.info" src="README" parse="no"/>
		<res id="shortcutSpec.xml" src="resource/installer/izpack/shortcutSpec.xml"/>
    </resources>

    <!-- 
        The panels section.
        We indicate here which panels we want to use. The order will be respected.
    -->
    <panels>
        <panel classname="HelloPanel"/>
        <panel classname="InfoPanel"/>
        <panel classname="LicencePanel"/>
        <panel classname="TargetPanel"/>
        <panel classname="PacksPanel"/>
		<panel classname="ShortcutPanel"/>
		<panel classname="InstallPanel"/>
        <panel classname="SimpleFinishPanel"/>
    </panels>

    <!-- 
        The packs section.
        We specify here our packs.
    -->
    <packs>
        <pack name="Base" required="yes">
            <description>The base files</description>
            <file src="README" targetdir="$INSTALL_PATH"/>
            <file src="LICENSE.txt" targetdir="$INSTALL_PATH"/>
			<!--<file src="logo-128x128.icns" targetdir="$INSTALL_PATH"/>-->
			<!-- The file will be parsed -->
			<fileset dir="build/app/application/lib" targetdir="$INSTALL_PATH/lib">
				<include name="**"/>
			</fileset>
			<fileset dir="build/app/application/extra" targetdir="$INSTALL_PATH/extra">
				<include name="**"/>
			</fileset>
			<fileset dir="build/app/application/scenario" targetdir="$INSTALL_PATH/scenario">
				<include name="**"/>
			</fileset>
			<file src="src" targetdir="$INSTALL_PATH"/>
        </pack>
	</packs>

	<!-- The native libraries to add. For Windows shortcuts -->
	<native type="izpack" name="ShellLink.dll">
		<os family="windows"/>
	</native>

</installation>
